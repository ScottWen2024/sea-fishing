<view class="page"><view class="card"><scroll-view scroll-x class="chip-row"><text wx:for="{{types}}" wx:key="*this" data-type="{{item}}" bindtap="switchType" class="chip">{{item}}</text></scroll-view><view class="must">MUST</view><view wx:for="{{data.must}}" wx:key="*this" class="item">{{item}}</view><view class="rec">REC</view><view wx:for="{{data.rec}}" wx:key="*this" class="item">{{item}}</view><view class="opt">OPT</view><view wx:for="{{data.opt}}" wx:key="*this" class="item">{{item}}</view></view></view>